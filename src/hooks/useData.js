import { useState, useEffect } from 'react'

export default function useData(getData, setData) {
    /* Recibe los datos y en el segundo la marca */
    const [newData, setNewData] = useState({})

    useEffect(() => {
        
    })

    return newData
}